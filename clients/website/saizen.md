# saizen

**Domain:** Website (no custom code). **Type:** N/A — no client-specific addon repo.
**Repo:** None — confirmed via `odoo-conf/saizen-17c.conf`'s `addons_path`, same pattern as
`kships`/`sanyo` (only `design-themes`, `server-brand`, `oca-repo/server-tools`).
**Host:** `ap-southeast-1` ("website only" workload, threaded). **Inactive** — service stopped
2026-08-17, client no longer active. Had a real domain + nginx vhost while running.

## Engagement / business context

Not yet documented — TODO: why the engagement ended, whether the domain/data should eventually
be decommissioned entirely.
