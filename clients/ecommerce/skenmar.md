# skenmar

**Domain:** E-commerce. **Type:** Runs on the shared `products/xmart.md` product, no bespoke
repo of its own.
**Repo:** `xmart` (confirmed via `odoo-conf/skenmar-17c.conf`'s `addons_path`) + `tpt_addons`
(a shared repo not yet located/cloned).
**Host:** `ap-southeast-1` ("website only" workload, threaded). Has a real domain + nginx vhost.

## Engagement / business context

Not yet documented — TODO: contacts, engagement history, what the store actually sells.
