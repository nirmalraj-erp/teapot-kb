# sanyo

**Domain:** Website (no custom code). **Type:** N/A — no client-specific addon repo.
**Repo:** None — confirmed via `odoo-conf/sanyo-17c.conf`'s `addons_path`, same pattern as
`kships`/`saizen` (only `design-themes`, `server-brand`, `oca-repo/server-tools`).
**Host:** `ap-southeast-1` ("website only" workload, threaded). Dormant (configured, not
started) — no domain/nginx vhost currently.

## Engagement / business context

Not yet documented — TODO: contacts, why it's configured but not running, launch plan if any.
