# chakra

**Domain:** Unclassified — repo not yet located, so no code signal for its domain.
**Host:** `ap-southeast-1` (purchase-heavy backend, worker mode — bumped to 768MB/1GB memory
tier for purchase reporting/import spikes). IP/port-only, no domain/nginx vhost.

"Purchase-heavy backend" doesn't obviously fit POS/e-commerce/academy/fleet — possibly a
distribution/wholesale customization. Re-tag once the repo is cloned and its domain is clear.

## Engagement / business context

Not yet documented — TODO: contacts, engagement history, addon repo location, industry/vertical.
